[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @voxly/db build && pnpm --filter @voxly/web build"

[deploy]
startCommand = "pnpm --filter @voxly/web start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
