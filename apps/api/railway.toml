[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @voxly/db build"

[deploy]
startCommand = "pnpm --filter @voxly/api start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
