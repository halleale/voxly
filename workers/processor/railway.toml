[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @voxly/processor build"

[deploy]
startCommand = "node workers/processor/dist/index.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
